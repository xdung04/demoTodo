name: Test Jira Update

on:
  workflow_dispatch:

jobs:
  jira-update:
    runs-on: ubuntu-latest
    steps:
      - name: Update Jira issue status (hardcoded)
        run: |
          ISSUE_KEY="TES-4"
          echo "📌 Updating Jira issue: $ISSUE_KEY"
          curl -X POST \
            -H "Authorization: Bearer ${{ secrets.JIRA_TOKEN }}" \
            -H "Accept: application/json" \
            -H "Content-Type: application/json" \
            --data '{
              "transition": {
                "id": "31"
              }
            }' \
            https://dungssunny412.atlassian.net/rest/api/3/issue/$ISSUE_KEY/transitions
